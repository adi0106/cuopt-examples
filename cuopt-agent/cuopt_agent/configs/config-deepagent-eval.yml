# SPDX-FileCopyrightText: Copyright (c) 2026, NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

general:
  telemetry:
    logging:
      console:
        _type: console
        # Set via DEBUG_LEVEL on the container (0=WARN, 1=DEBUG). See entrypoint.sh.
        level: ${LOG_LEVEL:-WARN}
    # tracing:
      # Phoenix tracing disabled.
      # phoenix:
      #   _type: phoenix
      #   endpoint: http://phoenix:6006/v1/traces
      #   project: cuopt-agent
      # Uncomment to use LangSmith for tracing with your own LangSmith account.
      # langsmith:
      #   _type: langsmith
      #   project: cuopt-agent
      #   endpoint: "${LANGSMITH_ENDPOINT:-https://api.smith.langchain.com}/otel/v1/traces"
      #   api_key: ${LANGSMITH_API_KEY}

llms:
  cuopt_llm:
    _type: nim
    model: minimaxai/minimax-m2.5
    api_key: ${NVIDIA_API_KEY}
    base_url: https://integrate.api.nvidia.com/v1/
    max_tokens: 16384    
    do_auto_retry: false

functions:
  healthcheck:
    _type: healthcheck

workflow:
  _type: deepagent_fn
  llm_name: cuopt_llm
  description: "Supply-chain optimization assistant powered by NVIDIA cuOpt"
  tools: []
  skills_dir:
    - skills
  agents_md_path: AGENTS.md
  skills:
    - ".skills"
  memory:
    - "AGENTS.md"
  workspace_dirs:
    - cuopt_agent/data/max_supply_what_ifs/input
  max_retries: 16
  retry_backoff_factor: 2.0
  retry_initial_delay: 1.0
  retry_max_delay: 120.0
  system_prompt: |
    You are a supply-chain optimization assistant powered by NVIDIA cuOpt.
    Solve the following optimization problem using Python and the cuOpt solver.
    Only write files in your working directory. Do not ask follow-up questions.
    Reference your skills to understand latest API usage before getting started.
    Answer simple questions directly.
    Report the optimal objective value as a single float in \boxed{...} on the last line.

eval:
  general:
    output_dir: /app/cuopt-agent/cuopt_agent/data/max_supply_what_ifs/eval/results
    verbose: true
    dataset:
        _type: csv
        file_path: /app/cuopt-agent/cuopt_agent/data/max_supply_what_ifs/eval/max_supply_what_ifs.csv
        id_key: id
        structure:
          question_key: question
          answer_key: answer
  evaluators:
    accuracy:
      _type: cuopt_objective
      tolerance: 1e-2